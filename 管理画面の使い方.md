# Personal gym Recipe 管理画面の使い方

## 1. GitHub専用キーを作る

1. GitHubへログインします。
2. 右上のプロフィール画像から「Settings」を開きます。
3. 左メニュー下部の「Developer settings」を開きます。
4. 「Personal access tokens」→「Fine-grained tokens」を開きます。
5. 「Generate new token」を押します。
6. Token nameに `Recipe blog manager` など分かりやすい名前を入力します。
7. Expirationで有効期限を選びます。安全のため90日程度を推奨します。
8. Resource ownerで `personal-gym-recipe` を選びます。
9. Repository accessは「Only select repositories」を選び、`personal-gym-recipe.github.io` だけを指定します。
10. Repository permissionsの「Contents」だけを「Read and write」にします。それ以外は変更しません。
11. 「Generate token」を押し、表示された `github_pat_` で始まるキーをコピーします。この画面を閉じると再表示できません。

## 2. 管理画面へ接続する

1. `https://personal-gym-recipe.github.io/admin/` を開きます。
2. GitHub専用キー欄へコピーしたキーを貼り付けます。
3. 「接続する」を押します。
4. 記事一覧が表示されたら接続完了です。

キーはブラウザーの `sessionStorage` にだけ一時保存されます。タブまたはブラウザーを閉じると消えます。共有パソコンでは、作業後に必ず「接続を終了」を押してください。

## 3. 新しい記事を投稿する

1. 「＋ 新しい記事」を押します。
2. タイトル、公開日、カテゴリー、URL用の名前、短い説明、本文を入力します。
3. URL用の名前は `squat-basics` のような半角英数字とハイフンがおすすめです。
4. 必要なら「画像を選ぶ」から5MB以下の画像を指定します。
5. 「GitHubへ保存」を押します。
6. 「保存しました」と表示されたら完了です。公開ページへの反映には通常1〜2分かかります。

## 4. 記事を編集・削除する

- 編集：左の記事一覧から記事を選び、修正後に「GitHubへ保存」を押します。
- 削除：記事を選んで「削除」を押し、確認画面で承認します。削除内容はGitHubの履歴から復元できます。

## 5. キーをなくした・漏れた場合

GitHubの「Settings」→「Developer settings」→「Personal access tokens」→「Fine-grained tokens」で該当キーを開き、「Revoke」または「Delete」で無効化します。その後、新しいキーを作ってください。
